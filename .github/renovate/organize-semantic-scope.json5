{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "semanticCommitScope": "{{parentDir}}",
  "packageRules": [
    {
      "description": "add semantic commit scope for terragrunt environments",
      "matchFileNames": [
        "terragrunt/*/*/**"
      ],
      "semanticCommitScope": "{{ lookup (split packageFileDir '/') 3 }}/{{ lookup (split packageFileDir '/') 4 }}"
    },
    {
      "description": "add semantic commit scope for k8s with helm subdir",
      "matchFileNames": [
        "k8s/**/helm/*"
      ],
      "semanticCommitScope": "{{ lookup (split packageFileDir '/') 4 }}/{{ lookup (split packageFileDir '/') 3 }}"
    },
  ]
}
