---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: &name test-longhorn-fast
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 333Mi
  storageClassName: longhorn-fast
  # volumeName: *name   # does not work unless pv and volume get pre-provisioned with that specified name
                        # -- otherwise dynamic provisioning creates a volume with pvc-UUID name
---
apiVersion: v1
kind: Pod
metadata:
  name: &name test-longhorn-fast
  namespace: default
spec:
  containers:
    - name: test
      image: busybox@sha256:fd8d9aa63ba2f0982b5304e1ee8d3b90a210bc1ffb5314d980eb6962f1a9715d
      command: ["sh", "-c", "while true; do echo $(date) >> /data/log.txt; sleep 5; done"]
      volumeMounts:
        - name: data
          mountPath: /data
  volumes:
    - name: data
      persistentVolumeClaim:
        claimName: *name