apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-param
  annotations:
    config.kubernetes.io/local-config: "true"
data:
  CLUSTER_ENVIRONMENT: poc
  DOMAIN_BASE: poc.iseja
  DOMAIN_TLD: net
  TMP: envs/example
