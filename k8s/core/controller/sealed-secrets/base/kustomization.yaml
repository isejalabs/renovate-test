apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sealed-secrets

resources:
  - helmrepository.yaml
  - helmrelease.yaml

configMapGenerator:
  - name: base-values
    files:
      - values.yaml

generatorOptions:
  disableNameSuffixHash: true
