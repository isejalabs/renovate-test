apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gateway

# have a separate kustomization without domain/hostname rewrites
# hence no components or transformers here

resources:
  - http-route-redirect.yaml
