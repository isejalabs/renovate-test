apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

components:
  - ../../../../../components/envs/poc

resources:
  - ../../base
  # - gw-tls-passthrough.yaml

patches:
  - path: gw-external.yaml
  - path: gw-internal.yaml
