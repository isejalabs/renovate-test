apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &name longhorn
spec:
  chart:
    spec:
      chart: *name
      sourceRef:
        kind: HelmRepository
        name: *name
        namespace: longhorn-system
      version: "1.12.1"
