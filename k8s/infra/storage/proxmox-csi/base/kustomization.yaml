apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: csi-proxmox

resources:
  - helmrelease.yaml
  - ocirepository.yaml

configMapGenerator:
  - name: base-values
    files:
      - values.yaml

generatorOptions:
  disableNameSuffixHash: true
