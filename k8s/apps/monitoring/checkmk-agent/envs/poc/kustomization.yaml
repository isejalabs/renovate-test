apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

components:
  - ../../../../../components/envs/poc

resources:
  - ../../base

patches:
  - path: patches/helm-version.yaml
  - path: patches/log-level.yaml