apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

components:
  - ../../../../../_components/poc

resources:
  - ../../base
  - egress-gw-policy.yaml

patches:
  - path: svc.yaml
  - path: deployment.yaml